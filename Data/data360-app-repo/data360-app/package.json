{
  "name": "data360-app",
  "version": "0.1.0",
  "private": true,
  "description": "Data 360 delivery app — React frontend + zero-dependency Node backend. Markdown projects + SQLite index, provider-agnostic AI (Claude/Gemini) grounded on the bundled reference.",
  "engines": { "node": ">=18 (>=22.5 recommended for the SQLite index)" },
  "scripts": {
    "setup": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server/src/index.js",
    "dev:server": "node --watch server/src/index.js",
    "dev:client": "cd client && npm run dev",
    "postinstall": "echo \"Run 'npm run setup' then 'npm run build', then 'npm start'.\""
  }
}
