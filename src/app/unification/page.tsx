import { PageHeader, Stub } from "@/components/ui";

export default function UnificationPage() {
  return (
    <div>
      <PageHeader
        title="Unification"
        sub="Identity-resolution ruleset design, derived from mapping identity fields."
      />
      <Stub
        title="Unification"
        blurb="Derive the match-rule ladder and reconciliation rules, run breaking-scenario checks, and confirm segment-readiness."
      />
    </div>
  );
}
