import { PageHeader, Stub } from "@/components/ui";

export default function SegmentsPage() {
  return (
    <div>
      <PageHeader
        title="Segments"
        sub="Segment catalog with cadence, calculated insights, and readiness checks."
      />
      <Stub
        title="Segments"
        blurb="Build the segment catalog — objective, criteria, DMOs, calculated insights, cadence, channel, and status."
      />
    </div>
  );
}
